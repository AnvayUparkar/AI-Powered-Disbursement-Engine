apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "dgcl.fullname" . }}-config
  labels:
    {{- include "dgcl.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
