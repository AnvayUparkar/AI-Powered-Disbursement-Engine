[pytest]
pythonpath = .
testpaths = tests
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    real_auth: use the real authentication dependency instead of the default-tenant bypass
    no_default_tenant: do not bind the default test tenant for this test
